INSTRUCCIONES IMPORTANTES:
Ve a tu panel de Supabase -> "SQL Editor" -> "New Query" y ejecuta exactamente este texto para agregar las nuevas columnas a la tabla clientes.

ALTER TABLE public.clientes
ADD COLUMN datos_facturacion TEXT,
ADD COLUMN seguimiento_pagos TEXT,
ADD COLUMN estado_cobro TEXT DEFAULT 'por_enviar';